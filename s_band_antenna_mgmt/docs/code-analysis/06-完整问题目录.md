# 06 完整问题目录（按文件索引）

> 便于逐文件整改。每行一个问题，格式：`行号 | 严重度 | 问题 | 详见`。
>
> 严重度：**P0** 阻断 · **P1** 严重（journal `high`）· **P2** 一般（`medium`）· **P3** 轻微（`low`）
>
> 行号已按当前磁盘版本复核。`cell_config.c` / `cell_config.h` 在工作流后被改过，其行号以本报告为准；其余 30 个 `.c`、32 个 `.h` 未变动。

**统计**：32 个 `.c` 中 **28 个有问题记录**；33 个 `.h` 中 **9 个**；另有 4 个脚本、7 份文档、5 个测试文件、2 个构建文件。

---

## 目录

[src/](#src) · [include/](#include) · [tests/](#tests) · [scripts/](#scripts) · [docs/ 与根目录文档](#docs-与根目录文档) · [构建文件](#构建文件) · [config/](#config) · [提交进仓库的运行数据](#提交进仓库的运行数据) · [零调用者函数清单](#零调用者函数清单) · [假实现清单](#假实现清单)

---

# src/

## `src/main.c`（515 行）

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:60-98` | P2 | 连接生命周期状态未复位 | [12 §P2-3](12-修复路线图.md) |
| `:101-164` | — | `on_tcp_data_received`：**全部 TCP 粘包/半包解帧逻辑在这里**（不在 `tcp_client.c`） | [07](07-架构与数据流.md) |
| `:120` | P2 | `memcpy(&msg_length, g_recv_buffer + 4, 4)` 直接按主机序解释线上字节，无 `le32toh`/`ntohl` | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:123` | P1 | 只校验 `msg_length` 在 15~8192，不校验与实收长度是否一致 | [04 §E1](04-P1-严重级问题.md#e1-用线上的-msg_length-而非实际-payload_len-计算解析范围) |
| `:145-148` | P0 | **无条件**置 `g_channel_established = true`，不看 dispatch 结果 | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:151` | P1 | `msg_handler_dispatch(&msg)` **返回值被丢弃**——全部 15 个 handler 的错误在此汇聚并消失 | [12](12-修复路线图.md) |
| `:167-172` | P1 | `on_uart_data_received` 是**空壳实现**（`(void)data;` + 一条 LOG_DEBUG），FPGA 对所有命令帧的响应被静默丢弃 | [04 §D2](04-P1-严重级问题.md#d2-133-字节遥测帧完全没有校验和--crc) |
| `:228-321` | P2 | 为 10 个复制粘贴桩 init 写了 **10 段恒假的错误处理** | [05 §P2-6](05-P2-一般级问题.md#普查-1恒返回-success-的函数假实现) |
| `:249-253` | P2 | 三个硬编码告警阈值（只在 `alarm_manager.c:446-447`、`:470` 两处被用，严格大于、无迟滞） | [05 §P2-5](05-P2-一般级问题.md#告警子系统) |
| `:255-258` | P3 | init 失败后 `goto cleanup`，而 cleanup 段 `:502` 无条件 destroy mutex | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:344-357` | P1 | `uart_rs422_open` 失败只打 `LOG_WARN("非致命错误,继续运行")` → 上注启动时必然段错误 | [04 §A2](04-P1-严重级问题.md#a2-devttyama2-打开失败时必然段错误) |
| `:422` | P1 | 缺 `signal(SIGPIPE, SIG_IGN)`（1 行修复） | [03 §D4](03-P0-阻断级问题.md#d4-tcp_client_send-无发送互斥) |
| `:430-479` | — | 1Hz 主循环的两条定时状态机 | [07](07-架构与数据流.md) |
| `:436-458` | — | 未建链时只发通道建立请求（符合"BBU 不会在握手前发心跳"的假设） | [03 §A4](03-P0-阻断级问题.md#a4-入站身份字段无一处参与判断) |
| `:461-478` | P0 | 心跳超时处理，兜底是**单向失效**的 | [03 §C5](03-P0-阻断级问题.md#c5-心跳超时兜底被-heartbeat_start-清零绕过) |
| `:481-514` | P0 | cleanup 销毁序列（detached 线程 + `logger_close()` 无锁 + destroy 在用锁） | [03 §D1](03-P0-阻断级问题.md#d1-退出路径-use-after-free) |
| `:485` | P1 | 正常路径先 stop 后 destroy（但 watchdog 的 stop 若未被调用则不安全） | [03 §D7](03-P0-阻断级问题.md#d7-watchdog-destroy-未初始化-mutex--二次-destroy) |
| `:497-498` | P0 | `uart_client_close` 后紧接 `uart_client_destroy` → 二次 join | [03 §D5](03-P0-阻断级问题.md#d5-二次-pthread_join-同一-tid日志实证) |
| `:502` | P3 | cleanup 段无条件 destroy mutex | — |
| `:504` | P2 | `version_manager_destroy` 不等待 detached 下载线程 | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |

## `src/version_manager.c`（问题最密集的文件）

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:31` | P3 | `create_directory_recursive` 对空字符串读 `tmp[-1]` 越界 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:67` | P3 | `calculate_sha256` 的 fread 循环不区分 EOF 与读错误 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:86` | **P0** | `popen("tar -xzf ...")` **shell 命令注入**（root） | [03 §A1](03-P0-阻断级问题.md#a1-shell-命令注入root-级远程命令执行) |
| `:128` | P2 | `read_checksum_from_txt` 用 readdir 找「第一个 .txt」，不认文件名 | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:367` | P3 | `save_version_metadata` 不转义、无临时文件+rename、无 fsync | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:406` | P3 | 用 `strstr(path,"metadata.txt")` 判定文件格式（脆弱耦合） | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:458` | P2 | `version.json` 用 sscanf 配固定格式串（前导恰好两个空格） | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:521-524`→`:528` | **P0** | curl 命令拼接后 popen，`store_path`/`file_name` 未净化 | [03 §A1](03-P0-阻断级问题.md#a1-shell-命令注入root-级远程命令执行) |
| `:594` / `:634` / `:843` | P2 | `(len > 0) ? SUCCESS : ERROR_GENERAL` 假成功模式 | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:652/669/677/690/700/708/724/757/785` | P3 | 9 个失败路径都不删 tar 包（成功路径 `:789` 才删） | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:656-657` | **P0** | 由 BBU 的 `file_name` 拼出 `tar_path` | [03 §A1](03-P0-阻断级问题.md#a1-shell-命令注入root-级远程命令执行) |
| `:687-692` | P0 | SHA-256 **算了却从不比对**（IE 14 里没有 checksum 字段） | [03 §C8](03-P0-阻断级问题.md#c8-sha-256-算了但从不比对) |
| `:695-696` | **P0** | 由 `task->version` 拼出 `version_dir`，无路径穿越检查 | [03 §A2](03-P0-阻断级问题.md#a2-路径穿越--任意文件上注) |
| `:718` | **P0** | `system("rm -rf \"<version_dir>\"")` 命令注入 + 任意目录删除 | [03 §A1](03-P0-阻断级问题.md#a1-shell-命令注入root-级远程命令执行) |
| `:762` | P0 | 下载线程**无锁**写 `task->version`（三方竞争） | [03 §D3](03-P0-阻断级问题.md#d3-version_manager-的-in_progress-闸门被自己的-memset-打掉) |
| `:777` | P0 | 把自己算出的 SHA 当作 `metadata.checksum` 写进 version.json | [03 §C8](03-P0-阻断级问题.md#c8-sha-256-算了但从不比对) |
| `:911` / `:914` | P2 | 符号链接切换**不是原子的**（注释 `:846` 却写「原子操作」） | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:960` | P1 | 只判 `!msg->payload`，不校验 `payload_len` 即强转 291/45 字节结构体 | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:962` | **P0** | 同上（download 路径，291 字节） | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:968` | P2 | **无任何磁盘空间检查**；`DOWNLOAD_ACK_REJECT_SPACE(=2)` 零使用点 | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:973` / `:981` / `:1085` | P2 | 3 个 handler 无 payload_len 前置检查即强转 IE 指针 | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:977`→`:987`→`:1040` | **P0** | `in_progress` 闸门被自己的 `memset` 打掉（无锁窗口） | [03 §D3](03-P0-阻断级问题.md#d3-version_manager-的-in_progress-闸门被自己的-memset-打掉) |
| `:993` / `:1014` | **P0** | `task->version` 直接来自 BBU 的 `ie_req->file_ver` | [03 §A2](03-P0-阻断级问题.md#a2-路径穿越--任意文件上注) |
| `:1072` | P2 | 下载线程是 `pthread_detach` 的 | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:1079` | **P0** | activate 路径只判 `!msg->payload`（45 字节结构体） | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:1105` | P2 | 上注期间**不停业务、不与下载互斥**，固件侧**无任何回滚机制** | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:1125` | P1 | 固件激活调 `load_version_metadata()`，但官方脚本打的包没有 metadata.txt → **必然失败** | [04 §B1](04-P1-严重级问题.md#b1-用官方脚本打的固件包激活必然失败) |
| `:1142` | **P0** | 激活应答在 `:1156` 上注启动**之前**发出 | [03 §C2](03-P0-阻断级问题.md#c2-固件上注激活应答在上注启动之前发出) |
| `:1156` | P1 | `fpga_firmware_injection_start` 不检查 init 是否执行过 | [04 §A2](04-P1-严重级问题.md#a2-devttyama2-打开失败时必然段错误) |
| `:1157-1160` | **P0** | 显式容忍注入失败 | [03 §C2](03-P0-阻断级问题.md#c2-固件上注激活应答在上注启动之前发出) |
| `:1167` | P3 | 写 `/tmp/antenna_mgmt_restart_pending` 标记文件，**无人读取**；且 `PrivateTmp=true` | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:1176` | P2 | 消息处理线程里同步 `sleep(3)` | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:1179` | P1 | `system("systemctl restart antenna-mgmt.service &")` 在消息处理线程里 | [04 §B2](04-P1-严重级问题.md#b2-软件激活的重启方式有三处问题) |
| `:1189` / `:1200` / `:1231` | P2 | `version_get_current` / `list_installed` / `rollback` **零调用者** | [05 §普查6](05-P2-一般级问题.md#普查-6额外发现约-20-个已实现但零调用者的函数) |
| `:1250` | P2 | `destroy` 不等待、不取消 detached 下载线程 | [05 §P2-1](05-P2-一般级问题.md#p2-1-版本管理与固件包) |
| `:847` | — | `switch_to_version`：版本回滚能力**完整实现**（含符号链接切换 + `rollback.json` 落盘）**却不可达** | [05 §普查6](05-P2-一般级问题.md#普查-6额外发现约-20-个已实现但零调用者的函数) |

## `src/cell_config.c`（当前 766 行，**工作流后被改过**）

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:13-21` | **P0** | `has_any_freq()` 只看 `active && freq_count > 0`，**不看是否真下发过** → 僵尸记录支撑发射 | [03 §C4](03-P0-阻断级问题.md#c4-小区配置去重陷阱现场触发-541-次) |
| `:239-314` | P1 | `wait_for_antenna_mode_cycle` **最长阻塞 5 秒**，跑在 TCP recv 线程上 | [08 §D.3](08-并发与锁审计.md) |
| `:241-242` | — | `MAX_WAIT_MS = 5000`、`POLL_INTERVAL_MS = 100` | [11](11-现场日志实证.md) |
| `:250` | — | `fpga_handler_get_status` 失败即返回 `ERROR_GENERAL` | — |
| `:257-261` | — | 只对业务模式和频谱监测模式做验证，其他模式直接 `return SUCCESS` | — |
| `:282-287` | P2 | 模式**没变化**时只 LOG_WARN 就 `return SUCCESS`（注释「可能天线已经配置好了，不算错误」） | — |
| `:377` | **P0** | `if (!mode_check_passed)` 分支入口 | [03 §C4](03-P0-阻断级问题.md#c4-小区配置去重陷阱现场触发-541-次) |
| `:402-415` | **P0** | 模式检查失败**仍写入本地表**并置 `active=true` | [03 §C4](03-P0-阻断级问题.md#c4-小区配置去重陷阱现场触发-541-次) |
| `:418-419` | — | `*result = CONFIG_RESULT_FAILURE; break;` | — |
| `:423` | — | `bool need_send_to_antenna = true;` | — |
| `:425-426` | P3 | 注释写「全局检查」但只扫 `freqs[cell_idx]`（当前小区） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:426-441` | **P0** | 去重循环只比 dl/ul/bw，**不比 beam_id、不查是否下发过** | [03 §C4](03-P0-阻断级问题.md#c4-小区配置去重陷阱现场触发-541-次) |
| `:444-476` | P3 | 内层 `freq_idx` **遮蔽**外层同名变量（`-Wshadow` 未开） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:493-527` | — | 步骤5：下发 + 放锁 + 等待模式周期 + 重加锁 | [08](08-并发与锁审计.md) |
| `:507` / `:516` / `:522` | P1 | **放锁 → 阻塞等待 → 重加锁**（在 TCP recv 线程上） | [08 §D.3](08-并发与锁审计.md) |
| `:510` | P1 | 调用 `wait_for_antenna_mode_cycle()` | — |
| `:528-532` | **P0** | else 分支：只调 `update_tx_control()` 就回 **SUCCESS** | [03 §C4](03-P0-阻断级问题.md#c4-小区配置去重陷阱现场触发-541-次) |
| `:539-548` | P3 | DELETE 分支：删除不存在的频点也**返回 success**（`:547` LOG_DEBUG） | — |
| `:634` | **P0** | TLV 循环缺 `ie_len < 4` 下界校验 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:639` | **P0** | TLV 边界检查（与 `:674` 推进语句配对） | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:660` | — | 调 `cell_config_apply_freq(&freq_cfg, &freq_result)` | — |
| `:674` | **P0** | `offset += ie_len` 推进语句 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:685` | P1 | `cell_config_handle_request` 是**空壳** | [05 §普查1](05-P2-一般级问题.md#普查-1恒返回-success-的函数假实现) |
| 跨小区 | P3 | 跨小区的相同频点**不会被去重** → 多小区同频部署时配置时间成倍增长（各等一次最长 5 秒） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |

> **修复版在 `rollback_backup/cell_config.{c,h}.modified`**（含 `sent_to_antenna` 字段，见 [02 §4.2](02-方法与数据来源.md)）。

## `src/fpga_firmware_injector.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:14-21` | — | 前向声明 | — |
| `:64` | P2 | `start()` 不重置 `current_packet_in_segment` → `get_progress` 返回陈旧数据 | [05 §P2-3](05-P2-一般级问题.md#p2-3-固件上注) |
| `:78` | P0 | 线程是 `pthread_detach` 的 | [03 §D1](03-P0-阻断级问题.md#d1-退出路径-use-after-free) |
| `:86` / `:99` / `:124` | P1 | `get_progress` / `_abort` / `_wait` **零调用者** | [04 §A3](04-P1-严重级问题.md#a3-progress_callback-永远为-null--进度上报是结构性死代码) |
| `:117` | P2 | **没检查** `uart_rs422_send_frame` 返回值（而 `:474` 检查了） | [05 §P2-6](05-P2-一般级问题.md#总体定性结论) |
| `:155` | P0 | cleanup 只 `sleep(2)` | [03 §D1](03-P0-阻断级问题.md#d1-退出路径-use-after-free) |
| `:166` | P0 | 就 `pthread_mutex_destroy`（detached 线程可能仍在跑） | [04 §A5](04-P1-严重级问题.md#a5-cleanup-在-detached-线程可能仍在运行时销毁-mutex) |
| `:255-270` | P1 | `transition_state` 后线程退出，不做业务恢复 | [04 §A4](04-P1-严重级问题.md#a4-重构reconfig成功后不做任何业务恢复) |
| `:295` | P2 | `total_segments` 计算（`file_size==0` 时为 0） | [05 §P2-3](05-P2-一般级问题.md#p2-3-固件上注) |
| `:307` | P2 | **整个固件文件一次性 malloc** 算 CRC16 → OOM 风险 | [05 §P2-3](05-P2-一般级问题.md#p2-3-固件上注) |
| `:448` | P2 | **无条件 `sleep(20)`**，且不可中止 | [04 §A7](04-P1-严重级问题.md#a7-handle_transfer_start-无条件-sleep20且不可中止) |
| `:474` / `:483` | P1 | `send_transfer_abort` 把 3-9 帧**发了两遍** | [04 §A6](04-P1-严重级问题.md#a6-send_transfer_abort-把同一个-3-9-中止帧在线上发了两遍) |
| `:502` | P2 | `file_size==0` → 跳过数据传输直接发 3-7；`file_type` 不做范围校验 | [05 §P2-3](05-P2-一般级问题.md#p2-3-固件上注) |
| `:588` | P2 | `tcflush(TCIOFLUSH)` 直接操作裸 fd，绕过封装，清不掉应用层环形缓冲 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:666` / `:685` | P1 | `progress_callback` 判空/调用，但**全工程无一处赋值**，也无 setter API | [04 §A3](04-P1-严重级问题.md#a3-progress_callback-永远为-null--进度上报是结构性死代码) |
| `:719` | P2 | `TRANSFER_END` / `RECONFIG` 阶段**完全没有重试** | [05 §P2-3](05-P2-一般级问题.md#p2-3-固件上注) |
| `:843-846` | P1 | 重构成功后只 `transition_state(COMPLETED)` 然后退出 | [04 §A4](04-P1-严重级问题.md#a4-重构reconfig成功后不做任何业务恢复) |
| 状态机整体 | — | **每步都校验 FPGA 应答码并重试**——本项目错误处理做得最好的地方 | [05 §双标证据](05-P2-一般级问题.md#3-双标证据最关键) |

## `src/uart_rs422_client.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:23` | P1 | `logic_corrected.md` 的 5 个方案只落地 3、半落地 1、未落地 2 | [04 §F1](04-P1-严重级问题.md#f1-docsfpga_injection_logic_correctedmd-描述的当前代码与真实代码严重不符) |
| `:69-72` | P3 | hex dump 用 `sprintf` 而非 `snprintf`，安全性依赖硬编码 64 字节上限 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:81-83` | P1 | 无条件 memcpy 覆盖 `received_frame` → 一次解析出多帧时**前面的帧被静默丢弃** | [04 §C6](04-P1-严重级问题.md#c6-一次-pkfinderparse-里到达多个完整帧时前面的帧被静默丢弃) |
| `:167` | P2 | open 用了 `O_NDELAY`，此后**从未 fcntl 清除** | [04 §C5](04-P1-严重级问题.md#c5-发送重试从帧头整帧重写不从短写偏移续写) |
| `:187-217` | — | 按设备路径判定并配 8O1（`logic_corrected.md` 方案3 **已落实**） | [04 §F1](04-P1-严重级问题.md#f1-docsfpga_injection_logic_correctedmd-描述的当前代码与真实代码严重不符) |
| `:190-191` | P2 | 置 `PARENB\|PARODD`（发送侧产生奇校验位） | [04 §C4](04-P1-严重级问题.md#c4-8o1-奇校验模式下清掉-inpck) |
| `:214` / `:216` | P2 | `c_iflag &= ~(INPCK \| ISTRIP)` **关闭输入校验检查** | [04 §C4](04-P1-严重级问题.md#c4-8o1-奇校验模式下清掉-inpck) |
| `:222` | P3 | `options.c_cc[VTIME] = 0.1` 浮点赋给 `cc_t` → **静默截断为 0** | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:249` | P3 | `close` 在 `!is_open` 时提前 return → **两把互斥锁永不 destroy** | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:278-280` | P2 | 发送重试**从帧头整帧重写**，不从短写偏移续写 | [04 §C5](04-P1-严重级问题.md#c5-发送重试从帧头整帧重写不从短写偏移续写) |
| `:323` | P2 | `recv_frame` 进入时只清 `frame_ready`，不清 rbuf 也不复位 pkfinder | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:385-394` | P2 | 环形缓冲部分写入时**静默丢弃尾部字节**（只在 `put_len==0` 时才 Clear） | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:419` | P1 | 把 `pkfinderParse` 的「数据还没收全」当成**致命解析错误** | [04 §C1a](04-P1-严重级问题.md#c1a-pkfinderparse-把数据还没收全当成致命错误) |
| `:434-475` | P2 | `send_and_wait` **不是原子事务**；应答匹配只比命令码，不比序列号/APID | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:444` | — | `send_frame` 内部取放 `send_mutex`（第一步就是 `uart_rs422_send_frame`） | [04 §A6](04-P1-严重级问题.md#a6-send_transfer_abort-把同一个-3-9-中止帧在线上发了两遍) |
| `:451` | — | `recv_frame` 内部取放 `recv_mutex` | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |

## `src/pkfinder.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:60` | P1 | 包头扫描上界 `i < (len - headLen + 1)` **无符号下溢** → 大范围越界读 | [04 §C1b](04-P1-严重级问题.md#c1b-包头扫描循环上界无符号下溢--大范围越界读) |
| `:84-87` | P1 | 定长包分支 `return 1` 时**不写 `*fndLen`** | [04 §C2](04-P1-严重级问题.md#c2-pkfinderfind-定长包分支不写-fndlen) |
| `:101-104` | P3 | 变长包两条 `return 1` 路径也不给 `*fndLen` 赋值 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:130` | — | 变长分支**有**赋值（对比） | — |
| `:148` | P1 | `pkLen` 初始化为 0，但 while 第二轮起带**上一轮残值** | [04 §C2](04-P1-严重级问题.md#c2-pkfinderfind-定长包分支不写-fndlen) |
| `:152` | P1 | `pk_fres` 初始化为 `ERROR(-1)` | [04 §C1a](04-P1-严重级问题.md#c1a-pkfinderparse-把数据还没收全当成致命错误) |
| `:164` | — | `while ((fndIndex+headLen)<=datLen)` —— **巧合挡住**了 `:60` 的下溢 | [04 §C1b](04-P1-严重级问题.md#c1b-包头扫描循环上界无符号下溢--大范围越界读) |
| `:167-171` | P1 | 「没找到包头」break → 返回 ERROR | [04 §C1a](04-P1-严重级问题.md#c1a-pkfinderparse-把数据还没收全当成致命错误) |
| `:172-183` | P1 | 「找到包头但长度不足」break → 返回 ERROR | [04 §C1a](04-P1-严重级问题.md#c1a-pkfinderparse-把数据还没收全当成致命错误) |
| `:174` | P1 | `if (pkLen>parseBufLen)` 读的是未初始化/过期的 `pkLen` | [04 §C2](04-P1-严重级问题.md#c2-pkfinderfind-定长包分支不写-fndlen) |
| `:189` | P1 | 只有 `chkExec` 返回 TRUE 时才置 `OK(0)` | [04 §C1a](04-P1-严重级问题.md#c1a-pkfinderparse-把数据还没收全当成致命错误) |
| `:17-38` | P3 | include 了 20+ 行与功能完全无关的头（`linux/can.h` 等） | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |

## `src/uart_client.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:72` | — | `options.c_cc[VTIME]` 正确写法（整数 1）——**对比 `uart_rs422_client.c:222`** | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:95` | — | 显式传 `&attr` 设栈大小（对比 `tcp_client.c:123` 用默认） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:125-127` | P2 | `read` 缓冲 1024 字节、重组缓冲 2048 字节；三个 static 变量属函数本身（不可重入） | [04 §D5](04-P1-严重级问题.md#d5-接收线程节奏撑爆内核-tty-缓冲) |
| `:167` | P1 | 帧头搜索 `header_pos < remaining - 1`，`remaining==1` 时**丢弃最后一个 0xEB** | [04 §D3](04-P1-严重级问题.md#d3-帧头搜索循环在-remaining1-时丢弃最后一个字节) |
| `:171` / `:221` | P1 | 帧完整性只靠 `0xEB90` / `0x5555` 两个魔数 | [04 §D2](04-P1-严重级问题.md#d2-133-字节遥测帧完全没有校验和--crc) |
| `:179` | P1 | `processed_bytes = frame_offset;` 无条件丢弃唯一字节 | [04 §D3](04-P1-严重级问题.md#d3-帧头搜索循环在-remaining1-时丢弃最后一个字节) |
| `:205` | P3 | 帧长度上限用 `sizeof(frame_buffer)=2048` 而非协议上限 | [05 §P3-1](05-P2-一般级问题.md#p3-1-数值与类型) |
| `:227-251` | P0 | 只有 `expected_frame_len == 133 && length_field == 127` 走遥测解析，**其他所有帧走空壳回调** | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:232` | P3 | 第三个函数内 static | [05 §P3-2](05-P2-一般级问题.md#p3-2-线程安全) |
| `:246-252` | P1 | 对非 133 字节的帧调 `on_data_received`（空壳） | [04 §D2](04-P1-严重级问题.md#d2-133-字节遥测帧完全没有校验和--crc) |
| `:284` | P2 | 每轮固定 `usleep(10ms)` + 每条日志两次 `fflush` → 撑爆内核 tty 缓冲 | [04 §D5](04-P1-严重级问题.md#d5-接收线程节奏撑爆内核-tty-缓冲) |
| `:299-322` | — | `uart_poll_thread`：1s 状态轮询 | [07](07-架构与数据流.md) |
| `:320` | P1 | 发送失败只打一条 ERROR，**无连续失败计数、无超时判定、不触发告警** | [04 §D1](04-P1-严重级问题.md#d1-fpga_handler_get_status-不判新鲜度--fpga-掉线后永久上报陈旧遥测) |
| `:376` | P2 | 置 `running=false` | [05 §P3-2](05-P2-一般级问题.md#p3-2-线程安全) |
| `:378-381` | P2 | **立刻 `close(fd)` 并置 -1** | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:385-390` | P0 | 才 join，且**不把两个 `pthread_t` 字段清零** | [03 §D5](03-P0-阻断级问题.md#d5-二次-pthread_join-同一-tid日志实证) |

## `src/fpga_handler.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:14` | P2 | `memset` 清零包含 `pthread_mutex_t` 的结构体（`:17` 再 init） | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:40` | P1 | `status_valid` **一旦置 true 就永远不会回退** | [04 §D1](04-P1-严重级问题.md#d1-fpga_handler_get_status-不判新鲜度--fpga-掉线后永久上报陈旧遥测) |
| `:41` | P1 | `last_update_time` 被写入但**全项目无任何地方读取** | [04 §D1](04-P1-严重级问题.md#d1-fpga_handler_get_status-不判新鲜度--fpga-掉线后永久上报陈旧遥测) |
| `:73` | P1 | `get_status` 只判「是否曾收到过一帧」，**不判新鲜度** | [04 §D1](04-P1-严重级问题.md#d1-fpga_handler_get_status-不判新鲜度--fpga-掉线后永久上报陈旧遥测) |
| `:112` | P1 | CPRI 工作模式原样下发（0 基/1 基三方差 1） | [04 §E3](04-P1-严重级问题.md#e3-cpri-工作模式的取值语义三方整体差-1) |
| `:117` | P2 | 名为 `cpri_mode` 的函数发 `FPGA_MSG_CPRI_CONFIG`(0x02) —— **msg_id 对调** | [04 §E4](04-P1-严重级问题.md#e4-fpga_send_cpri_mode-与-fpga_send_cpri_config-的-msg_id-被对调) |
| `:137` | P2 | 名为 `cpri_config` 的函数发另一个 ID，**日志文本跟着错** | [04 §E4](04-P1-严重级问题.md#e4-fpga_send_cpri_mode-与-fpga_send_cpri_config-的-msg_id-被对调) |
| `:152-183` | P1 | `fpga_send_beam_num` 两帧非原子 | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:167` | P2 | **忽略第一帧**的发送结果（只有 `:179` 第二帧的结果被返回） | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:226-243` | P1 | `fpga_send_loopback` **只发 1 字节 type**，port/beam/period 从未下发 | [04 §E组](04-P1-严重级问题.md#e-管理面业务) |
| `:284` | P3 | `localtime()` 非线程安全；年份 `&0xFF` 会回绕 | [05 §P3-2](05-P2-一般级问题.md#p3-2-线程安全) |
| `:365-374` | **P0** | `fpga_send_raw` → `uart_client_send` **直写 /dev/ttyAMA0** | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:377` | P1 | CPRI 工作模式下发点 | [04 §E3](04-P1-严重级问题.md#e3-cpri-工作模式的取值语义三方整体差-1) |
| 状态缓存整体 | — | **锁纪律良好，是本项目做得最对的一块** | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |

## `src/fpga_protocol.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:64` | P3 | `uint8_t quotient = freq_khz / 50000;` → **12.8 GHz 上限，静默溢出** | [05 §P3-1](05-P2-一般级问题.md#p3-1-数值与类型) |
| `:65` | **待确认** | `*2` 是**未文档化的魔数**（低 24 位 LSB 到底是 500 Hz 还是 1 kHz？） | [09](09-协议一致性.md) |
| `:99` | P2 | 编码端 `length_field = msg->payload_len` | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:167` | P2 | 解码端 `payload_len = (length_field >= 2) ? length_field - 2 : 0` —— **语义差 2 字节** | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:234` | P2 | 对 **uint8_t** 字段做 `NTOHS` → 越界读 1 字节 | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |

## `src/fpga_watchdog.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:41-42` | P2 | mmap **完全没有页对齐处理**（靠白名单校验侥幸安全） | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:78-90` | P2 | `configure_iomux` 无锁读-改-写 | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:82` | P2 | IOMUX 清位掩码硬编码 `0xF << mux_func_bit`（隐含假设 `pull_bit == func_bit + 2`） | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:85` | P2 | 用 `GPIO_FUNCTION_VALUE(0x2)` 置 bit[9:8] → **配置项形同虚设** | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:165-173` | P2 | `gpio_set_direction` 无锁读-改-写 | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:206-214` | P2 | `gpio_set_value` 无锁读-改-写 | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:278-289` | P2 | `watchdog_feed` 无锁读-改-写 | [05 §P2-4](05-P2-一般级问题.md#p2-4-fpga-南向) |
| `:321` | P3 | 「Watchdog fed (count=%u)」DEBUG 日志**被注释掉** | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:325` | P1 | 喂狗线程（SIGPIPE 终止进程期间会停止 → 可能触发硬件复位） | [03 §D4](03-P0-阻断级问题.md#d4-tcp_client_send-无发送互斥) |
| `:436` | **P0** | `destroy` **不检查/不 join 喂狗线程就 munmap** → use-after-unmap | [03 §D7](03-P0-阻断级问题.md#d7-watchdog-destroy-未初始化-mutex--二次-destroy) |
| `:439-448` | P0 | 直接 munmap 两段 MMIO 并置 NULL，**全程不看 enabled** | [03 §D7](03-P0-阻断级问题.md#d7-watchdog-destroy-未初始化-mutex--二次-destroy) |
| `:456` | P0 | 销毁 mutex | [03 §D7](03-P0-阻断级问题.md#d7-watchdog-destroy-未初始化-mutex--二次-destroy) |

## `src/heartbeat.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:40-53` | **P0** | `heartbeat_start()` **清零 `bbu_heartbeat_miss_count` 与时间基准** | [03 §C5](03-P0-阻断级问题.md#c5-心跳超时兜底被-heartbeat_start-清零绕过) |
| `:47` | P0 | 被 msg 2 每次调用（`msg_handler.c:94` → `:124` → 此处） | [03 §C5](03-P0-阻断级问题.md#c5-心跳超时兜底被-heartbeat_start-清零绕过) |
| `:120` | — | `last_bbu_heartbeat_time = time(NULL)`（收到心跳，正常） | [11](11-现场日志实证.md) |
| `:121` | — | `bbu_heartbeat_miss_count = 0`（收到心跳，正常） | — |
| `:129-159` | — | `heartbeat_check_timeout` 全体 | [11](11-现场日志实证.md) |
| `:138` | P2 | `time_t now = time(NULL);`（**墙钟**，非单调时钟） | [11 §时钟回跳](11-现场日志实证.md) |
| `:139` | P1 | `elapsed = now - last_bbu_heartbeat_time`（**有符号相减**，回跳后为负） | [11 §时钟回跳](11-现场日志实证.md) |
| `:142` | P1 | `elapsed >= HEARTBEAT_INTERVAL_SEC` 回跳后**长期为假** | [11 §时钟回跳](11-现场日志实证.md) |
| `:143` | P0 | `bbu_heartbeat_miss_count++` | [03 §C5](03-P0-阻断级问题.md#c5-心跳超时兜底被-heartbeat_start-清零绕过) |
| `:146` | — | `>= HEARTBEAT_TIMEOUT_COUNT`（20 次，约 60 秒） | [11](11-现场日志实证.md) |
| `:147-148` | P3 | 超时日志**严重误导**：应记录首次丢失时刻并打印总静默时长 | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:154` | — | `last_bbu_heartbeat_time = now`（更新时间基准，避免重复计数） | [11](11-现场日志实证.md) |

## `src/msg_handler.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:32-48` | P2 | 分发表**缺 `MSG_PAAU_VERSION_QUERY(11)` / `ACK(12)`** | [05 §普查6](05-P2-一般级问题.md#普查-6额外发现约-20-个已实现但零调用者的函数) |
| `:56` | — | `msg_handler_dispatch` 入口 | [03 §A4](03-P0-阻断级问题.md#a4-入站身份字段无一处参与判断) |
| `:60`→`:62` | **P0** | paau_id 比对应插入的位置（当前**无**） | [03 §A4](03-P0-阻断级问题.md#a4-入站身份字段无一处参与判断) |
| `:67` | **P0** | 透传入口 → `transparent_msg.c:285` | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:78-80` | P2 | 未知 msg_id：`LOG_WARN` + `return ERROR_GENERAL`（返回值又被丢弃） | [03 §A7](03-P0-阻断级问题.md#a7-msg_id-无白名单) |
| `:89-128` | **P0** | msg 2 处理（需加幂等短路） | [03 §C5](03-P0-阻断级问题.md#c5-心跳超时兜底被-heartbeat_start-清零绕过) |
| `:94` | P0 | → `:124` → `heartbeat_start()` | [03 §C5](03-P0-阻断级问题.md#c5-心跳超时兜底被-heartbeat_start-清零绕过) |
| `:102` | P1 | 通道建立应答的 `result` 是**硬编码 0** | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:124` | P0 | `heartbeat_start()` —— 双方状态机永久错位的成因 | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:191` / `:223` | P2 | `(len > 0) ? SUCCESS : ERROR_GENERAL` 假成功 | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:231` | — | 调 `cell_config_handle_request(msg)` | [07](07-架构与数据流.md) |
| `:285` / `:308` / `:331` | — | **已有的正确 payload_len 校验写法**（照抄对象） | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:316` / `:339` / `:362` / `:377` | P1 | 审计日志的 `source_ip` 是 `config_get_string("BBU_IP","unknown")` —— **配置常量，不是实际对端** | [03 §A3](03-P0-阻断级问题.md#a3-北向链路零认证) |

## `src/channel_setup.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:18` | — | `#define IE_TYPE_VERSION_CHECK 14` | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:76` / `:414` | P3 | 用 `config_get_int` 读 PAAU_ID（**对比 `alarm_manager.c:290` 硬编码 0**） | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:222-401` | P1 | `channel_setup_handle_config`（需如实返回错误） | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:237` | **P0** | TLV 边界检查缺下界 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:243` | **P0** | TLV 循环（与 `:397` 配对） | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:296` | P1 | `fpga_send_cpri_config(mode)` 返回值**未检查** | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:301-344` | **P0** | `IE_TYPE_VERSION_CHECK` 完整分支 | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:303-305` | P2 | `type=0`（软件版本）时也执行 LOG_INFO，但**完全不解析任何字段** | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:308` | — | `if (ie_data_len >= 285 && version_type == 1)` | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:311-331` | — | memcpy 解析 result(4)/file_path(200)/file_name(16)/file_len(4)/file_time(20)/file_version(40) | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:333-334` | P2 | 只 LOG_INFO 打印 result | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:336-340` | **P0** | `result != 0` 只 `LOG_WARN("...may need update")`，**无任何后续动作** | [03 §C3](03-P0-阻断级问题.md#c3-ie-14-版本核对结果被丢弃) |
| `:359` / `:387` | P1 | 另 2 个 `fpga_send_*` 返回值**未检查** | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |
| `:397` | **P0** | TLV 推进语句 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:400` | P1 | `channel_setup_handle_config` 末尾**无条件 `return SUCCESS`** | [03 §C6](03-P0-阻断级问题.md#c6-通道建立应答-result-恒为字面量-0) |

## `src/status_query.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:71` | **P0** | 循环条件（永远成立） | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:76` | **P0** | 唯一一道检查 `req_offset + ie_len > request->payload_len`，`ie_len==0` 时为假 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:81` | P2 | `ie_data_len = ie_len - 4` **下溢成 65532**，但没被用来做边界检查 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:96` | P3 | 波束数量**硬编码 16** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:97-100` | P3 | 注释写着「可以从 FPGA 状态获取实际波束数量」，真实赋值**被注释掉** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:117` / `:135` / `:163` / `:180` / `:197` / `:214` / `:223` | P3 | **7 个 `add_ie` 调用点全部丢弃返回值**（波束循环内那处最多执行 16 次） | [05 §普查2](05-P2-一般级问题.md#普查-2返回值被忽略的调用) |
| `:157-158` | P2 | 遥测不可用时**伪造健康值**：本振频率填 21000（=2.1GHz）、本振状态填 0（锁定） | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:176` | P2 | 时钟状态填 0（同步） | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:220` | **P0** | `calib_resp[0] = 0; /* 0=成功 */` —— **硬编码，完全不读 FPGA** | [03 §C7](03-P0-阻断级问题.md#c7-校准结果查询硬编码成功) |
| `:233` | **P0** | `req_offset += ie_len` 推进语句 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:240` | P3 | 日志「IE 个数」用 `offset/10` 估算，对混合长度 IE **完全错误** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |

## `src/param_config.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:11` | P2 | `g_cpu_stat_period`（默认 60）**只被回显，不影响任何实际统计周期** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:32-34` | P2 | 时间配置校验覆盖 second/minute/hour/day/month，**漏了 year** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:43` | P2 | `tm_ti…`（year 是 uint16_t，不写 RTC、不下发 FPGA） | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:128-134` | P1 | CPRI 工作模式校验只拒绝 `>3`，接受 **0~3** 原样下发 | [04 §E3](04-P1-严重级问题.md#e3-cpri-工作模式的取值语义三方整体差-1) |
| `:163` | P1 | 用**线上的 `msg_length`** 而非实际 `payload_len` 计算解析范围 → 可越界读 | [04 §E1](04-P1-严重级问题.md#e1-用线上的-msg_length-而非实际-payload_len-计算解析范围) |
| `:177-178` | P2 | while 循环 + `offset+4>payload_len` 检查 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:190` | **P0** | `offset+ie_length>payload_len` 检查，`ie_length==0` 时不成立 | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:198` / `:207` / `:216` | P3 | 把 payload 偏移处**直接强转结构体指针** → 非对齐访问 UB | [05 §P3-1](05-P2-一般级问题.md#p3-1-数值与类型) |
| `:199` / `:208` / `:217` | P2 | 多 IE 配置**立即落地**（settimeofday / 写全局 / 下发 FPGA），无原子性、失败不回滚 | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:235` | **P0** | `offset += ie_length` → **无限循环** | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:244` | P2 | `static uint8_t resp_buffer[256]` → `cpri_payload_free` 的 free 分支是**堆破坏地雷** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:276` | P2 | 赋给 `response->payload`（注释声称「线程安全」） | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |

## `src/alarm_manager.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:63-69` | — | `alarm_state_tracker_t`（边沿检测用的「上一次状态」镜像，与 `records[]` 是两套独立机制） | [07](07-架构与数据流.md) |
| `:87` / `:89` | P3 | init 失败路径上 **destroy 未初始化的 mutex** | [03 §D7](03-P0-阻断级问题.md#d7-watchdog-destroy-未初始化-mutex--二次-destroy) |
| `:105` | P3 | 同上 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:142` | P2 | `find_free_record` —— `alarm_clear` 后槽位**立刻被复用** | [04 §E2](04-P1-严重级问题.md#e2-跨解锁窗口持有记录指针reported-标志可能打到错误的告警上) |
| `:168-169` | P2 | 告警表满时**直接丢弃新告警**（只 LOG_ERROR，不进表、不上报、不计数） | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:204` | P3 | **注释与代码相反** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:206` + `:209-211` | P2 | `alarm_report_to_bbu` 返回值被丢弃后**无条件** `reported = true` | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:243` | P2 | `clear_time` 写了也**无人读** | [04 §E2](04-P1-严重级问题.md#e2-跨解锁窗口持有记录指针reported-标志可能打到错误的告警上) |
| `:255` | P2 | `alarm_clear` 置 `active=false` → **无告警历史，清除即销毁** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:290-292` | P3 | 主动上报的 `paau_id`/`bbu_id`/`port_num` **全硬编码 0** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:308` | P2 | `localtime()` 非线程安全且未判空，**跨两个线程并发调用** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:349` | P2 | `alarm_report_to_bbu` 本身**只看编码**，`(len>0)?SUCCESS:ERROR` | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:353-388` | — | `alarm_report_all_pending`：重传机制**已正确实现**（`:371` 检查 ret 才置位）却因上游提前置位而**永不触发** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:364` / `:369` / `:372` | P1 | 解锁 → 阻塞上报 → 重加锁 → 写 `record->reported`（**跨解锁窗口持指针**） | [04 §E2](04-P1-严重级问题.md#e2-跨解锁窗口持有记录指针reported-标志可能打到错误的告警上) |
| `:446-447` | P2 | 温度比较用**硬编码门限**，忽略 FPGA 遥测自带的 6 个门限字段 | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:470` | P3 | `fault_count > threshold` 裸阈值判断，**无迟滞** → 告警风暴 | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:487` | P0 | `alarm_trigger_version_*` **已预留但未接线** | [03 §C2](03-P0-阻断级问题.md#c2-固件上注激活应答在上注启动之前发出) |
| `:535-537` | P2 | 调 `alarm_check_over_temperature` | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:543-549` | P1 | **备路光纤告警被注释掉，但旧日志证明它曾经生效** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:547-548` | P2 | 备路光口(4~7)检测被注释 → `optical_sync_loss[8]` 后半**永远 false** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| 数据结构 | P2 | `alarm_record_t` 里**没有 severity/level 字段**，IE 1001 的 134 字节载荷里也没有位置放它；无 ack API、无 ack 字段 | [09](09-协议一致性.md) |

## `src/tcp_client.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:7-12` | P3 | `CONNECTING` 状态**从未被赋值**（死状态） | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:11` | — | 前向声明 | — |
| `:44` | — | `pthread_create`（`tcp_reconnect_thread`，T1） | [08](08-并发与锁审计.md) |
| `:106` | P2 | 之后应加 `setsockopt(SO_RCVTIMEO/SO_SNDTIMEO)` + `TCP_NODELAY`（当前 `:168 recv` 与 `:230 send` 都可能**无限阻塞**） | [12](12-修复路线图.md) |
| `:111-114` | P1 | 连接成功后应调 `getpeername` + `inet_ntop`（当前**没有**） | [03 §A3](03-P0-阻断级问题.md#a3-北向链路零认证) |
| `:123` | P3 | `pthread_create` 使用**默认栈属性**（对比 `uart_client.c:95`） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:168` | — | `recv`（T2 主循环） | [07](07-架构与数据流.md) |
| `:172` | — | → `on_data_received` → `main.c:101` | [07](07-架构与数据流.md) |
| `:218-226` | P0 | 只用 `state_mutex` 保护「取 sockfd」这一小段 | [03 §D4](03-P0-阻断级问题.md#d4-tcp_client_send-无发送互斥) |
| `:228` | P2 | `tcp_client_send` **无发送互斥锁** | [03 §D4](03-P0-阻断级问题.md#d4-tcp_client_send-无发送互斥) |
| `:229-236` | **P0** | 真正的分包 send 循环**完全无锁** | [03 §D4](03-P0-阻断级问题.md#d4-tcp_client_send-无发送互斥) |
| `:230` | P1 | `send(sockfd, …, 0)` **未带 `MSG_NOSIGNAL`** → SIGPIPE 终止进程 | [03 §D4](03-P0-阻断级问题.md#d4-tcp_client_send-无发送互斥) |

## `src/reset.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:21-22` | P3 | `RESET_ERR_PERMISSION` / `OUT_OF_RANGE` 是死码 → **本项目根本不存在任何权限模型** | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:156` / `:158` | P3 | 有错误描述映射、有告警码 70002，但**全项目无一处 return** | [03 §A4](03-P0-阻断级问题.md#a4-入站身份字段无一处参与判断) |
| `:210` | P1 | 之后应重新 `strncpy(parse_result.source_i…)`（**memset 顺序 bug**） | [12](12-修复路线图.md) |
| `:239-240` | P3 | 留着更早版本的注释痕迹 | [03 §C1](03-P0-阻断级问题.md#c1-复位动作被注释掉仍返回成功) |
| `:252` | **P0** | `LOG_INFO("Service will restart in 2 seconds...")` —— **假日志** | [03 §C1](03-P0-阻断级问题.md#c1-复位动作被注释掉仍返回成功) |
| `:253` | **P0** | 真的 `sleep(2)`（在消息处理线程里） | [03 §C1](03-P0-阻断级问题.md#c1-复位动作被注释掉仍返回成功) |
| `:254` | **P0** | `// system("systemctl restart antenna-mgmt.service &");` —— **被注释掉** | [03 §C1](03-P0-阻断级问题.md#c1-复位动作被注释掉仍返回成功) |
| `:271` / `:272` | **P0** | 硬复位同样 | [03 §C1](03-P0-阻断级问题.md#c1-复位动作被注释掉仍返回成功) |
| `:275` | **P0** | `return SUCCESS` | [03 §C1](03-P0-阻断级问题.md#c1-复位动作被注释掉仍返回成功) |
| 演进状态 | P3 | 从纯回声桩演进到转发给 FPGA，**停在了半路**：FPGA 命令加上了，`system()` 却被注释回去 | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |

## `src/rs422_protocol.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:8` | P1 | `RS422_SYNC_WORD_ACK` = `0x1ACF` | [04 §G2](04-P1-严重级问题.md#g2-rs422_decode_frame-只接受-0x1acf而-encode-只产生-0xeb90) |
| `:11` | P2 | `static uint16_t g_seq_counter = 0;` 无锁、非 volatile | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:15` | P2 | `rs422_protocol_init` 清 0 后**只增不减**，14 位静默回绕 | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:74-133` | — | 帧格式（C 实现，**日志实证 C 是对的**） | [09](09-协议一致性.md) |
| `:78` | P3 | `if (!frame_buf \|\| frame_buf_size < RS422_HEADER_S…)` —— NULL + 大 size **绕过检查** | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:89` | P1 | 写入 `htons(RS422_SYNC_WORD_CMD)` = `0xEB90` | [04 §G2](04-P1-严重级问题.md#g2-rs422_decode_frame-只接受-0x1acf而-encode-只产生-0xeb90) |
| `:103` | P2 | `g_seq_counter++` 读-改-写三步；**重发路径从不回滚** | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:149` | P1 | `if (sync != RS422_SYNC_WORD_ACK)` → **5 个往返测试必然失败** | [04 §G2](04-P1-严重级问题.md#g2-rs422_decode_frame-只接受-0x1acf而-encode-只产生-0xeb90) |
| `:173-174` | P2 | `actual_data_len = data_len + 1`；长度校验只要求 `frame_len >= 8 + actual_data_len` | [04 §C3](04-P1-严重级问题.md#c3-rs422_decode_frame-的-payload_len-无符号下溢) |
| `:204` | P2 | `uint32_t pl_len = actual_data_len - 2;` **之前没判 `>= 2`** → 下溢成 0xFFFFFFFE/0xFFFFFFFF | [04 §C3](04-P1-严重级问题.md#c3-rs422_decode_frame-的-payload_len-无符号下溢) |
| `:240-256` | P3 | 1-4/1-5 版本查询指令**已完整实现但无生产调用者** → 上注后不校验 FPGA 实际生效版本 | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:291-293` | P1 | `rs422_build_file_data` 实现是 **7 个参数** | [04 §G1](04-P1-严重级问题.md#g1-单元测试与当前源码已不同步无法编译) |
| `:299-303` | P3 | 不校验 `data_len` 是否超过 `RS422_MAX_PAYLOAD_SIZE`，也不校验 uint16 回绕 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |

## `src/transparent_msg.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:49` | **P0** | `is_transparent_message` 形参是 `uint16_t msg_id` → **65536 别名截断** | [03 §A6](03-P0-阻断级问题.md#a6-is_transparent_message-形参截断) |
| `:49` | P3 | 把**两个方向都判定为透传**，所以 221-230 会走到这里 → BBU 误发 221 会被当作成功处理 | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:74` | P0 | `transparent_parse_result_t.msg_id` 需同步改 uint32_t | [03 §A6](03-P0-阻断级问题.md#a6-is_transparent_message-形参截断) |
| `:99` / `:109` | P0 | 原型需同步 | [03 §A6](03-P0-阻断级问题.md#a6-is_transparent_message-形参截断) |
| `:108` | P0 | `transparent_msg_parse` 的 `uint16_t msg_id` 需改 | [03 §A6](03-P0-阻断级问题.md#a6-is_transparent_message-形参截断) |
| `:145-150` | — | **全项目唯一正确的 TLV 下界校验写法**（照抄对象） | [03 §B1](03-P0-阻断级问题.md#b1-tlv-下界校验缺失--接收线程死循环) |
| `:187` | P0 | IE 1602 必须存在 | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:257` | P0 | `handle_transparent_bbu_to_paau` 的 `uint16_t msg_id` 需改 | [03 §A6](03-P0-阻断级问题.md#a6-is_transparent_message-形参截断) |
| `:282` | P0 | 内容非全零即可；P2 建议此处加令牌桶限速 | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:283` | P3 | 日志说 "via RS422"，**实际走 ttyAMA0** → 排障找错串口 | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:285` | **P0** | `fpga_send_raw(...)` —— **绕过全部 FPGA 命令封装** | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:290` | P2 | 建议加令牌桶 | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `:295-306` | **P0** | **无条件自动回显** → 反射原语，把攻击者的 1024 字节弹回 | [03 §A5](03-P0-阻断级问题.md#a5-透传通道可裸写-fpga) |
| `send_transparent_paa…` | P3 | **栈占用偏大** | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |

## `src/log_upload.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:60` / `:104` | P2 | `(len > 0) ? SUCCESS : ERROR_GENERAL` 假成功 | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:120-122` | P3 | URL 拼接**不处理 `store_path` 结尾斜杠** → 产生 `//`（**现场实证**：`/upload/` → `ftp://…//upload/…`） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:133-137`→`:142` | **P0** | curl 命令拼接后 `popen`，`store_path` 未净化 → **shell 注入** | [03 §A1](03-P0-阻断级问题.md#a1-shell-命令注入root-级远程命令执行) |
| `:163` | P3 | 用 `WEXITSTATUS` 但**全项目无一处 include `<sys/wait.h>`**；未先用 `WIFEXITED` 判断 | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:289` | **P0** | 只查 `!msg->payload`，不校验长度（204 字节结构体） | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:306` | P2 | 无 payload_len 前置检查即强转 IE 指针 | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |

## `src/loopback.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:82` / `:88` | P1 | `test_period`/`port_number`/`beam_number` **已校验但从未下发** | [04 §E组](04-P1-严重级问题.md#e-管理面业务) |
| `:88-89` | P3 | `MAX_BEAM_NUMBER(255)` 定义了但**从未使用**；注释「验证波束号（暂时不限制上限…」 | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:177` / `:179` | P3 | 有错误描述映射、有告警码 60002，但**无一处 return** | [03 §A4](03-P0-阻断级问题.md#a4-入站身份字段无一处参与判断) |
| `:270` | P2 | `(len > 0) ? SUCCESS : ERROR_GENERAL` 假成功 | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:314` / `:316` | P3 | 陈旧桩注释与横幅：「现阶段仅记录日志，不执行实际业务」 | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:333-343` | **P0** | 环回结果 = **UART write 返回值** | [04 §E组](04-P1-严重级问题.md#e-管理面业务) |

## `src/phased_array_calib.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:92` | P2 | `(len > 0) ? SUCCESS : ERROR_GENERAL` 假成功 | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:126-134` | **P0** | 校准应答的 `result` = `fpga_send_calibration()` 的 **UART write 返回值** | [03 §C7](03-P0-阻断级问题.md#c7-校准结果查询硬编码成功) |

## `src/alarm_query.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:108` / `:179` | P2 | 多条告警**共用同一 `serial_num`**，无条数/结束标记 | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:145` / `:213` | P2 | `(len > 0) ? SUCCESS : ERROR_GENERAL` 假成功 | [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) |
| `:280` | P2 | 「只报最近 24 小时」过滤 → **把活跃超过一天的告警藏起来** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |

## `src/param_query.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:31-32` | P3 | `static prev_total`/`prev_idle` **无锁保护**，首次调用返回 0 | [05 §P3-2](05-P2-一般级问题.md#p3-2-线程安全) |
| `:111` | P2 | CPU 统计周期是**「只回显不生效」的假配置** | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:350` | P2 | 输出功率按通道查询是**假实现**（`rf_channel` 被忽略，无条件返回全局值） | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:357` | P2 | `rf_channel` 只在日志里用了 | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |
| `:518-519` | P2 | 认真解析出 `rf_channel` 并传给 handler | [05 §P2-5](05-P2-一般级问题.md#p2-5-管理面业务) |

## `src/config.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:57` | P3 | `config_load` 只把 `g_config_count` 置 0，**不 memset `g_config_items`** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:80` | P3 | `strncpy(dst, src, sizeof(dst)-1)` **不保证 NUL 终止** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:88` | — | 写入 `g_config.bbu_ip`（`main.c:403` 传给 `tcp_client_init`） | [07](07-架构与数据流.md) |
| `:89` | P3 | `bbu_port` 是 uint16_t 却由 int 直接赋值，**无范围校验** | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| `:106` | P3 | `log_max_size` 是 size_t 由 int 赋值 | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |
| 解析器 | P3 | 只认整行 `#` 为注释、不处理引号、不支持 section、**不支持行内注释**；最多 `MAX_CONFIG_ITEMS=64` 项，key 64B | [05 §P3-5](05-P2-一般级问题.md#p3-5-语义与日志) |

## `src/fpga_to_cpri.c`（**整个文件 247 行是死代码**）

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:36` | P3 | `add_ie_to_payload` 溢出时返回错误但**调用方全部忽略** | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:80` | P1 | `uint32_t beam_status = 0;` **硬编码假数据** + 占位告警码 | [04 §D4](04-P1-严重级问题.md#d4-整个-fpga_to_cpric-是死代码且内部有硬编码假数据) |
| `:82`/`:91`/`:95`/`:99`/`:103`/`:140`/`:147`/`:150`/`:153`/`:156`/`:240` | P3 | **11 处**忽略 `add_ie_to_payload` 返回值，payload 指针/offset 状态不一致 | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| 三个导出函数 | P1 | `fpga_to_cpri_status_response` / `_param_response` / `_alarm_report` **零调用者**，但 `Makefile:18` 的 wildcard 仍编进产物 | [04 §D4](04-P1-严重级问题.md#d4-整个-fpga_to_cpric-是死代码且内部有硬编码假数据) |

## `src/cpri_protocol.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:67-103` | — | `msg_type_names[]` 表（可复用作 msg_id 白名单查找） | [03 §A7](03-P0-阻断级问题.md#a7-msg_id-无白名单) |
| `:129-141` | P2 | `cpri_encode_message` 同样按主机序处理（无 `htole32`） | [05 §P3-6](05-P2-一般级问题.md#p3-6-脚本与部署) |
| `:167` | P1 | `msg_length` **直接来自报文** | [04 §E1](04-P1-严重级问题.md#e1-用线上的-msg_length-而非实际-payload_len-计算解析范围) |
| `:174`→`:177` | **P0** | msg_id 白名单应插入的位置（当前**无**范围校验） | [03 §A7](03-P0-阻断级问题.md#a7-msg_id-无白名单) |
| `:178` | P1 | 发现 `msg_length` 与实收长度不符时**只打日志不拒绝** → 长度校验形同虚设 | [03 §A8](03-P0-阻断级问题.md#a8-入站-payload-长度零校验) |
| `:19/19` | — | `cpri_encode_message` **19/19 检查返回值**（做对的样本） | [05 §双标证据](05-P2-一般级问题.md#3-双标证据最关键) |

## `src/firmware_package.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:146` | P3 | `while ((bytes_read = fread(buffer, 1, sizeof(buff…` **不检查返回值/ferror** | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:186` | P3 | `off_t`(64位) 强转 `uint32_t`，**无溢出检查** → >4GiB 静默截断 | [05 §P3-1](05-P2-一般级问题.md#p3-1-数值与类型) |
| `firmware_parse_metadata` | P3 | 允许字段**重复计数** | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |

## `src/rBuf.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|
| `:17-38` | P3 | include 了 20+ 行与功能完全无关的头 | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `:42-98` | P2 | `rBufPut` 空间不足时**返回实际写入长度**而不是失败 | [05 §P2-2](05-P2-一般级问题.md#p2-2-rs-422-传输层) |
| `:48-51` / `:106-109` / `:145-148` | — | Put/Get/Remove **都判了 NULL**（防御风格正确的样本） | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |
| `:169` | P3 | `rBufNBytes`/`rBufIsFull`/`rBufClear`/`rBufInit` **缺少 NULL 检查**（风格不一致） | [05 §P3-3](05-P2-一般级问题.md#p3-3-资源与错误路径) |

## `src/rng.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:9` | P3 | **源文件编码是 GBK/ISO-8859**，而 `rBuf.c`/`pkfinder.c` 是 UTF-8 | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |

## `src/logger.c`

| 行号 | 级别 | 问题 | 详见 |
|---|---|---|---|
| `:126` | — | `logger_init` | — |
| `:216` / `:222` | P3 | `logger_log` 内部**每次都 fflush**（所以 `logger_flush` 功能上不缺，但声明无实现） | [05 §P3-4](05-P2-一般级问题.md#p3-4-死代码与陈旧残留) |
| `logger_close()` | **P0** | **无锁**，在退出路径上被调用 | [03 §D1](03-P0-阻断级问题.md#d1-退出路径-use-after-free) |
| `g_log_mutex` | **P0** | 临界区覆盖**阻塞文件 I/O** → 全局串行化点，放大信号处理器死锁窗口 | [03 §D6](03-P0-阻断级问题.md#d6-signal-handler-自死锁) |

---

# include/

| 文件:行 | 级别 | 问题 |
|---|---|---|
| `cpri_protocol.h:10-45` | — | `cpri_msg_type_t` 枚举（msg_id 白名单的数据源） |
| `cpri_protocol.h`（76 行） | — | 北向协议的全部真相：15 字节消息头 |
| `fpga_protocol.h:40-139` | — | 遥测帧字段偏移 |
| `fpga_protocol.h:97` | P1 | `cpri_work_mode`：「1=普通，2=级联，3=主备，4=反向分担」（**与 `param_config.h:30` 和代码 0~3 三方差 1**） |
| `fpga_protocol.h:100` | **P0** | `calib_result`「0=成功，其他=失败」—— **字段存在且已解码，但全项目零读取者** |
| `fpga_protocol.h:145` | P1 | 133 字节遥测帧定义处 —— **完全无校验和/CRC** |
| `param_config.h:30` | P1 | IE 字段：「1:普通 2:级联 3:主备 4:负荷分担」 |
| `param_query.h:73` | P2 | 注释声称 year 范围 1970-2099，但代码**不校验 year** |
| `version_manager.h:21` | P2 | `DOWNLOAD_ACK_REJECT_SPACE(=2)` 定义，**全项目零使用点** |
| `version_manager.h:49` | P2 | `MAX_FILENAME_LEN=16` **装不下项目自己规定的包名**（21 字符） |
| `transparent_msg.h:74` | **P0** | `transparent_parse_result_t.msg_id` 需改 uint32_t |
| `transparent_msg.h:99` / `:109` | **P0** | 原型需同步改 uint32_t |
| `transparent_msg.h:129` | **P0** | `is_transparent_message` 形参 `uint16_t` → 截断 |
| `tcp_client.h:7-12` | P3 | `CONNECTING` 是**死状态** |
| `tcp_client.h:15` | P1 | 应加 `peer_ip[INET_ADDRSTRLEN]` 字段（当前没有） |
| `tcp_client.h` | P0 | **没有发送锁**（grep 确认） |
| `uart_client.h:27` | P3 | `bool running;` **未声明 volatile** |
| `uart_rs422_client.h:17` | P3 | `fd` 是**公开字段**，上层不加锁就访问 |
| `fpga_firmware_injector.h:65` | P1 | `progress_callback` 字段声明，**全工程无一处赋值，无 setter API** |
| `rs422_protocol.h:70` / `:85` | P3 | 约 8 个死定义；`RS422_MAX_FRAME_SIZE=1012` 与真实最大帧长的对应**只存在于巧合** |
| `rs422_protocol.h:289-291` | P1 | `rs422_build_file_data` 声明是 **7 个参数**（测试只传 6 个） |
| `rs422_protocol.h:343-345` | P2 | `rs422_seq_counter_rollback` / `reset` 是**死代码** |
| `reset.h:21` | P3 | `RESET_ERR_PERMISSION` / `OUT_OF_RANGE` 死码 |
| `loopback.h:28` | P3 | 同上 |
| `logger.h:13` | P3 | `logger_flush()` **有声明但无实现、无调用者** |
| `gtypes.h:8` | P3 | `ULONG = unsigned long`，宽度随 ABI 变化 |
| `gtypes.h:26` | P3 | **`TRUE=0` / `FALSE=-1`** 与 stdbool 并存 |
| `rng.h:64`/`:82`/`:99`/`:123`/`:158`/`:167` | P3 | 六处引用**项目内不存在的函数 `tmr()`** |
| `cell_config.h:87` | **P0** | **修复版有 `bool sent_to_antenna;`，当前版本没有** |
| `alarm_manager.h:63-69` | — | `alarm_state_tracker_t` 定义 |
| `alarm_manager.h` | P2 | `alarm_record_t` **没有 severity/level 字段** |

---

# tests/

| 文件:行 | 级别 | 问题 |
|---|---|---|
| `test_rs422_protocol.cpp:611` | P1 | `rs422_build_file_data` 的 **9 处调用都少传一个实参** → **编译不过** |
| `test_rs422_protocol.cpp:167` | P3 | `EncodeFrameBasic` 断言依赖主机字节序假设 |
| `test_rs422_protocol.cpp` Decode* | P1 | **5 个编解码往返测试必然失败**（同步字不匹配） |
| `test_firmware_package.cpp:228` | P2 | **永真断言** `EXPECT_TRUE(ret == SUCCESS \|\| ret != SUCCESS)` |
| `test_fpga_firmware_injector.cpp:35` | P2 | `MOCK_METHOD4` vs 真实 **7 参数**；`extern "C"` 包装层丢弃 `recv_buf`/`recv_len` |
| `test_fpga_firmware_injector.cpp:254` | P3 | 对已销毁的互斥量重复 `pthread_mutex_destroy` / cleanup 后再 init |
| `test_fpga_injector_advanced.cpp:302` | P2 | Invoke lambda **按引用捕获栈上局部变量**，在 detached 线程执行 → 悬垂引用 + 数据竞争 |
| Mock 应答 | P2 | **用错同步字** → 25+ 用例必然失败 |
| 段数断言 | P2 | 把「1MB 段」当成「1000 字节包」 |
| `sleep(20)` | P2 | 使测试在时间上**不可能跑完** |
| 从不设定期望的 Mock | P2 | — |

---

# scripts/

| 文件:行 | 级别 | 问题 |
|---|---|---|
| `create_version_package.sh:33` | P3 | `show_help` 内部 `exit 0` → 「缺少必需参数」的**退出码是 0** |
| `create_version_package.sh:48-51` | P3 | 检测到 `-v`/`-b` 缺失时先 echo 错误再调 show_help |
| `create_version_package.sh:99-109` | P1 | 打出的包里**只有 `antenna_mgmt.bin` 和 `checksum.txt`，没有 `metadata.txt`** → 固件激活必然失败 |
| `create_version_package.sh:106` | P2 | 生成 `antenna_${VERSION}.tar.gz` = **21 字符 > `MAX_FILENAME_LEN=16`** |
| `deploy-autostart.sh:144-147` | P3 | 写完 network 配置只 `enable`，**不 restart/reload** → 本次开机内不生效 |
| `deploy-autostart.sh:212-222` | P3 | `set -e` 脚本中间有 `read -p` 交互确认 → 非交互环境**静默失败退出** |
| `restart-after-activate.sh:23` | P3 | 用 `systemctl list-unit-files \| grep -q`；**该脚本全项目无人调用** |
| `antenna-mgmt.service:19` | P1 | `Restart=always`（SIGPIPE 终止后会拉起，但期间喂狗停止） |
| `antenna-mgmt.service:35` | P1 | **`PrivateTmp=true`** → `/tmp/antenna_mgmt_restart_pending` 写在私有命名空间，外部脚本看不到 |
| `antenna-mgmt.service` | **P0** | **`User=root`** —— 命令注入的后果被放大到 root |
| `test/fpga_injector_test.py:26` | P1 | 实现的是**旧协议版本**，与 C 代码**十个维度全部不符**（`APID_CONTROL=0x03A0` / `APID_DATA=0x0…`） |
| `README.md:9-13` / `:297` | P3 | 文档覆盖不全 + **4 个「相关文档」链接指向不存在的目录** |

---

# docs/ 与根目录文档

| 文件 | 级别 | 状态 |
|---|---|---|
| `docs/api`、`docs/guides`、`docs/images`、`docs/procedurefile` | — | **四个目录全空** |
| **仓库内是否存在协议规格书 / ICD** | — | **不存在** |
| `docs/fpga_injection_logic_corrected.md:9-22` | P1 | 把两层结构（1MB 段 + 1000 字节包）描述成一层；递增的是 `current_packet_in_segment` 不是 `current_segment` |
| `docs/fpga_injection_logic_corrected.md:10` | P1 | 单包超时是 **5 秒**，文档说 15 秒 |
| `docs/fpga_injection_logic_corrected.md:100-108` | P1 | 声称的内容与代码不符 |
| `docs/fpga_injection_logic_corrected.md:117-183` | P1 | 5 个方案：**落地 3、半落地 1、未落地 2** |
| `docs/fpga_injection_issue_analysis.md` | P1 | 同批文档，与代码不符 |
| `fpga_firmware_injection_integration_complete.md:369` | P1 | **"100% 完成"结论不成立**；§2.1 说改的是 `version_manager.c`"第 828-851 行"，实际在 **1144-1184**（828 行现在是 malloc 错误分支）；§2.2 说 `main.c`"第…"也对不上 |
| `docs/VERSION_MANAGEMENT.md:73` | P2 | 与代码**直接矛盾** |
| `docs/VERSION_MANAGEMENT.md:105` | **P0** | 声称 BBU 会传 `expected_checksum` —— **IE 14 里没有这个字段（文档虚构）** |
| `docs/VERSION_MANAGEMENT.md:193` | P2 | 「升级前确保有足够的磁盘空间（至少100MB）」—— **对人说的，不是代码做的** |
| `docs/VERSION_MANAGEMENT.md` | P1 | 承诺的「上注结果指示」**从未实现** |
| `version_deployment_guide.md` | P1 | **虚构了协议字段与不存在的能力** |
| `version.json` vs `metadata.txt` | P2 | **同名字段不同含义**（语义冲突） |
| `FPGA_WATCHDOG_README.md` | P1 | **全面过期**：函数签名、配置项名、GPIO 端口号都是旧版本 |
| `KERNEL_PANIC_FIX.md`（175 行） | P1 | 记录一次因访问错误 IOMUX 物理地址 `0x28180100` 导致的 ARM Asynchronous SError 内核 panic。**该文档描述的修复已被回退**——当前代码重新启用了 IOMUX 写入，只是地址改成了 `0x28180214` |
| `scripts/README.md:297` | P3 | 覆盖不全 + 4 个死链接 |
| `FPGA_TEST_GUIDE.md` | P1 | 帧长数字**与两个实现都不符**（纯文档算术错误） |
| `docs/LastTest.log:883` / `:890-899` | P2 | 测试证据来自**旧版代码**（10000 字节文件"总段数: 10"、"数据段 1/10 发送成功 (1000 字节)"） |
| `docs/LastTest.log:979` | P3 | `[ FAILED ] FPGAAdvancedTest.DataSegmentSendFailure (2103 ms)` |
| `FPGA_Telemetry_Definition.md` | — | **缺失**，但影响被二进制取证大幅降低（DWARF 已独立验证字段布局） |

---

# 构建文件

| 文件:行 | 级别 | 问题 |
|---|---|---|
| `CMakeLists.txt:44-50` | P2 | `antenna_mgmt_lib` 的 SOURCES 只有 `rs422_protocol.c` / `logger.c` / `fpga_firmware_injector.c` / … → **覆盖率统计的分母只有 4 个文件** |
| `CMakeLists.txt:45` | P1 | 测试库**不含 `uart_rs422_client.c`**（也不含 `pkfinder.c`、`rBuf.c`）→ 整个串口传输层**零自动化测试覆盖**，且有链接错误 |
| `Makefile:18` | P1 | wildcard 把死代码 `fpga_to_cpri.c` **编进 antenna_mgmt** |
| `Makefile:31-34` | P3 | **无头文件依赖跟踪**（没有 `-MMD`/`-MP`，也没把 `include/*.h` 列进依赖）→ 改头文件后 `make` 不重编 |
| `Makefile` CFLAGS | P3 | 只有 `-Wall -Wextra`，**未开 `-Wshadow`** |
| `Makefile` | P3 | **无交叉编译支持**，但仓库产物是 aarch64 |
| 三套构建入口 | P2 | 把报告写到**三个不同目录**，没一个是仓库里真实存在的 `reports/` |
| `.vscode` | — | 提供**零工具链信息** |

---

# config/

| 位置 | 级别 | 问题 |
|---|---|---|
| `config/antenna_mgmt.conf:50-52` | P3 | 三行写了**行内注释**（`LOG_MAX_SIZE=10485760  # 10MB`），而解析器**不支持行内注释** → 这三个键**靠 `atoi` 的前缀解析侥幸得到正确值**；任何用 `config_get_string` 取值的键若加行内注释就会把注释一并当成值 |
| `config/antenna_mgmt.conf` `PAAU_ID=0` | P3 | 存在此配置项，但 `alarm_manager.c:290-292` **硬编码 0 不读配置** |

---

# 提交进仓库的运行数据

| 位置 | 大小 | 说明 |
|---|---|---|
| `logs/antenna_mgmt.log*`（6 个文件） | **~57MB** | 2026-05-28 ~ 2027-01-01。**混合了至少两个构建，后半段与当前源码逐行吻合**。含 NUL 空洞（grep 必须加 `-a`）；时间戳非单调（BBU 通过 IE 11 反复校时，同文件混有 2025-05-09 / 2025-07-23 / 2026-xx / 2027-01-01）；**横跨 4 个不同 build 的 `cell_config.c`** |
| 根目录 `antenna_mgmt.log` | 57943 B | 2026-03-31，**确实是旧构建** |
| `antenna_mgmt`（二进制） | **692104 B** | 未 strip，aarch64。**取证价值极高** |
| 32 个 `.o` | — | **取证价值极高** |
| `reports/html`、`reports/json`、`reports/xml` | **全空** | 覆盖率数字无任何产物支撑 |
| 一个垃圾文件 | — | **文件名是误敲命令** |
| `rollback_backup/cell_config.{c,h}.modified` | 29924 / 4750 B | 2026-09-22 08:52，**P0-4 的修复版（已被回退）** |

> ⚠️ **清理前请先确认取证工作已完成**——正因为二进制未 strip、`.o` 都在，才能做出「源码-产物同源，可信度确证（proven）」的字节级判定。

---

# 零调用者函数清单

约 **20 个**已实现但零调用者（普查 6 额外发现）。已点名的：

| 函数 | 位置 | 影响 |
|---|---|---|
| `version_get_current` | `version_manager.c:1189` | BBU 查不到 PAAU 版本 |
| `version_list_installed` | `version_manager.c:1200` | 同上 |
| `version_rollback` | `version_manager.c:1231` | **版本回滚能力完整实现却不可达** → 升级失败后无法回滚 |
| `switch_to_version` | `version_manager.c:847` | 含符号链接切换 + `rollback.json` 落盘（被 rollback 调用，间接不可达） |
| `fpga_to_cpri_status_response` | `fpga_to_cpri.c` | 整个文件死代码 |
| `fpga_to_cpri_param_response` | `fpga_to_cpri.c` | 同上 |
| `fpga_to_cpri_alarm_report` | `fpga_to_cpri.c` | 同上 |
| `fpga_firmware_injection_get_progress` | `fpga_firmware_injector.c:86` | 进度上报不可达 |
| `fpga_firmware_injection_abort` | `fpga_firmware_injector.c:99` | 无法中止上注 |
| `fpga_firmware_injection_wait` | `fpga_firmware_injector.c:124` | 无法等待完成 |
| `rs422_build_version_query` | `rs422_protocol.c:240` | **上注后不校验 FPGA 实际生效版本** |
| `rs422_parse_version_ack` | `rs422_protocol.c:240-256` | 同上 |
| `rs422_seq_counter_rollback` | `rs422_protocol.h:343-345` | 序列号不回滚 |
| `rs422_seq_counter_reset` | 同上 | 同上 |
| `logger_flush` | `logger.h:13` | **有声明无实现** |
| `tmr()` | `rng.h` 六处注释引用 | **项目内根本不存在这个函数** |

**二进制取证确认**：**42 个死代码符号全部存在，无一被 gc 掉**；`.dynsym` 只导出 2 个已定义符号。另有 **7 个前序未列出的不可达函数**（全量普查补出）。

---

# 假实现清单

**38 个**（普查 1）：15 个纯桩 + 2 个空壳 `handle_request` + 21 个吞错误。

已点名的：

| 类型 | 位置 | 行为 |
|---|---|---|
| **硬编码假数据** | `status_query.c:220` | `calib_resp[0] = 0;` 校准永远成功 |
| **硬编码假数据** | `status_query.c:157-158`/`:176` | 遥测不可用时伪造「一切健康」 |
| **硬编码假数据** | `fpga_to_cpri.c:80` | `beam_status = 0` + 占位告警码 |
| **硬编码常量** | `status_query.c:96` | 波束数硬编码 16，真实赋值被注释 |
| **动作被注释** | `reset.c:254` / `:272` | `system()` 被注释，仍返回 SUCCESS |
| **空壳** | `main.c:167-172` | `on_uart_data_received`：`(void)data;` |
| **空壳** | `cell_config.c:685` | `cell_config_handle_request` |
| **假配置** | `param_query.c:111` | CPU 统计周期只回显不生效 |
| **假实现** | `param_query.c:350` | 输出功率按通道查询忽略 `rf_channel` |
| **未下发的参数** | `loopback.c:82`/`:88` | port/beam/period 校验了但从未下发 |
| **恒返回字面量** | `msg_handler.c:102` | 通道建立应答 result 恒 0 |
| **无条件 SUCCESS** | `channel_setup.c:400` | `channel_setup_handle_config` 末尾 |
| **write 冒充结果** | `loopback.c:333-343`、`phased_array_calib.c:126-134` | 结果 = UART write 返回值 |
| **12 处同构** | 见 [05 §普查4](05-P2-一般级问题.md#普查-4假成功语义命令已发出冒充操作已成功) | `(len > 0) ? SUCCESS : ERROR_GENERAL` |
| **10 个复制粘贴桩 init** | 普查 1 #1-#10 | `main.c:228-321` 为它们写了 10 段恒假的错误处理 |

**17 个遥测字段零读取者**（普查 5，复核结论：**完全正确，一个不差，无遗漏也无误报**），含：
- `calib_result`（`fpga_protocol.h:100`，byte 85）
- `loopback_result`（byte 99）
- 6 个告警门限字段（被 `main.c:249-253` 硬编码值取代）
- `last_update_time`（`fpga_handler.c:41`）
- `beam_count`（`status_query.c:97-100` 的真实赋值被注释掉）

**遥测字段利用率**：按语义字段 **70%（28/40）**，按语义字节 **81.2%（95/117）**。

---

## 相关章节

- [03-P0](03-P0-阻断级问题.md) · [04-P1](04-P1-严重级问题.md) · [05-P2/P3](05-P2-一般级问题.md)
- [12-修复路线图](12-修复路线图.md)
